version: '3.4'

services:
  nginx:
    build:
      context: .
      dockerfile: nginx/Dockerfile
    volumes:
      - ".:/var/www/app"
    ports:
      - '80:80'
    depends_on:
      - fpm

  fpm:
    image: slymuzzle/php:${VERSION}-dev
    volumes:
      - ".:/var/www/app"
